name: Build APK
on: push

jobs:
  android:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Build with Buildozer
        uses: zezis/buildozer-action@v1
        with:
          command: buildozer android debug
